{
  "name": "vtk-app-test",
  "version": "1.0.0",
  "description": "test-app using vtk.js",
  "main": "index.js",
  "scripts": {
    "build": "webpack --progress --mode=production",
    "dev": "webpack --progress --mode=development",
    "start": "webpack serve --progress --mode=development --static=dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "vtk.js": "^21.3.1"
  }
}
